import styled from '@emotion/styled';

import { Box as MBox } from '@mui/material';

export const MuiBox: any = styled(MBox)();
// export { default as MuiBox } from '@mui/material/Box';
// export const MuiBox = styled(Box)<BoxProps>() as typeof Box;
